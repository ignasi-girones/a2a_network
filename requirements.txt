# Instalar con: pip install -r requirements.txt
# Requiere Python 3.12+

# ── Core A2A protocol SDK ──────────────────────────────────────────────────────
a2a-sdk[http,sqlite]==1.0.0a0

# ── LLM (wrapper unificado para Groq, Gemini, Mistral, Cerebras, Ollama) ───────
litellm>=1.55

# ── MCP (Model Context Protocol) — tools server y cliente ─────────────────────
mcp>=1.0

# ── HTTP y servidor ASGI ──────────────────────────────────────────────────────
httpx>=0.27
uvicorn[standard]>=0.30

# ── Modelos de datos y configuración ──────────────────────────────────────────
pydantic>=2.0
pydantic-settings>=2.0

# ── SSE streaming ─────────────────────────────────────────────────────────────
sse-starlette>=2.0
packaging
